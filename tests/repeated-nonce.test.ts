import { recoverKeyFromRepeatedNonce } from '../src/attacks/recover-key';
import { describe, it, expect } from 'vitest';

describe('Repeated Nonce Recovery', () => {
  it('recovers key from two signatures with repeated nonce', () => {
    // Use known-good test vector or fixture
    // expect(recoverKeyFromRepeatedNonce(...)).toBe(...);
  });
});
