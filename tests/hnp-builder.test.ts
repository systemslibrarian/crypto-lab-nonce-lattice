import { buildHNP } from '../src/attacks/hnp-builder';
import { describe, it, expect } from 'vitest';

describe('HNP Builder', () => {
  it('builds correct equations for known input', () => {
    // Add fixture-based test
  });
});
